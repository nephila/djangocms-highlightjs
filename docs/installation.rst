============
Installation
============

At the command line::

    $ easy_install djangocms_highlightjs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms_highlightjs
    $ pip install djangocms_highlightjs